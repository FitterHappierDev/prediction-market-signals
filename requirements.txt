# Python 3.12 — pinned dependencies for the PM Signal Platform
# Source: Tech Design Appendix A. Versions chosen as the latest compatible patch
# of each spec'd major.minor at scaffold time. Adjust if install fails.

# --- Foundation ---
python-dotenv~=1.0
pydantic~=2.9
pydantic-settings~=2.5
PyYAML~=6.0

# --- Async HTTP ---
aiohttp~=3.10
httpx~=0.27

# --- Data ---
pandas~=2.2
numpy~=1.26
polars~=1.9
pyarrow~=17.0
duckdb~=1.1

# --- Time-series DB & cache ---
questdb~=2.0
redis~=5.1

# --- ML / statistics ---
lightgbm~=4.5
scikit-learn~=1.5
scipy~=1.14
statsmodels~=0.14
mlflow~=2.16
optuna~=4.0

# --- Blockchain & crypto ---
web3~=7.4
ccxt~=4.4

# --- Traditional-asset market data (Phase 3 Market Context Service) ---
# yfinance is a community wrapper around Yahoo Finance: free, no key,
# daily bars reliable; 1-min only for last 7 days. Acceptable for v1.
yfinance~=0.2

# --- Brokers ---
alpaca-py~=0.30
ib_async~=1.0

# --- Notifications ---
slack-sdk~=3.33
python-telegram-bot~=21.6

# --- Testing ---
pytest~=8.3
pytest-asyncio~=0.24
pytest-mock~=3.14

# --- Observability (optional) ---
grafana-client~=4.2
